# JCSQE〜初級〜 v4 テスト報告書

実施日: 2026-09-11
対象: 起動UX改善版 v4

## 1. 今回の不具合と対策

### 現象

アプリ起動のたびにユーザー名入力画面が表示され、問題演習までの操作が増えていた。

### UX方針

ユーザー名は学習そのものに必須ではないため、起動条件から完全に除外した。

- 初回: `ユーザー1` を自動生成 → ホームへ直行
- 2回目以降: 前回ユーザーを自動復元 → ホームへ直行
- 名前変更: 必要な場合だけ「ユーザー」画面で行う
- 複数ユーザー: 同じ端末を複数人で利用する場合だけ追加する
- 保存領域が利用不可: 入力画面へ戻さず、ホームを利用可能にして保存不可警告を表示

受入基準は「新規ユーザーがURLを開き、追加入力なし・1操作で『今日の5問』を開始できること」とした。

## 2. ロジック単体試験

結果: **10/10 PASS**

- パーセント計算
- 空履歴の統計
- 正誤集計
- 誤答後に正解した問題の誤答対象解除
- 分野別集計
- ランダム抽出の重複防止
- 弱点抽出
- 14日推移生成
- 未回答を含む採点
- セッション分野別集計

## 3. 起動・状態管理 結合試験

結果: **18/18 PASS**

- 新規起動でユーザー名入力を表示しない
- 新規起動で `ユーザー1` を自動作成
- 自動作成プロフィールを保存
- 再読込後も同じアクティブユーザーを復元
- 旧v3プロフィール・回答履歴を保持
- 不正なactiveProfileIdから既存プロフィールへ自動復旧
- localStorage利用不可でもホームを表示
- localStorage利用不可時に保存不可警告を表示
- ホームから1操作で「今日の5問」を開始
- 回答を保存
- 再起動相当で途中セッションを復元
- 複数プロフィール間で履歴を分離
- 名前変更を保存
- プロフィール削除後に残存プロフィールへ自動切替
- 主要5画面を描画
- 分野別21/21入口を起動
- 固定模試10/10回を各40問で起動
- ランダム模試40問の重複なし

加えて、コード上から旧 `first-name` / `first-start` / `renderOnboarding` が消えていることを検査し、表示される全 `data-action` に処理先が存在することを静的検査した。

## 4. 全体フロー 結合試験

結果: **7/7 PASS**

- 今日の5問: 5問回答 → 結果 → 学習分析
- 模擬試験: 次へ → 前へ → 問題ジャンプ → 回答 → 採点
- 名前未入力で2人目プロフィールを自動命名して追加
- 表示名変更
- 学習履歴・結果・途中セッション初期化
- JSONバックアップから新しい独立プロフィールとして復元
- JSON書き出し処理

## 5. PWA・静的構成試験

結果: **PASS**

- `index.html` が参照するローカルファイル: 欠落0
- Service Workerのプリキャッシュ対象: 欠落0
- `app.js`, `logic.js`, `questions.js`, `sw.js`: JavaScript構文検査PASS
- PWAキャッシュ名を `jcsqe-shokyu-v4-20260911` に更新
- `app.js`, `logic.js`, `questions.js`, `styles.css`, manifestに `?v=4` を付け、旧キャッシュの影響を低減
- Service Worker登録時 `updateViaCache: 'none'` を使用
- ナビゲーションはネットワーク優先、オフライン時はキャッシュへフォールバック

## 6. 問題データ回帰試験

結果: **PASS**

問題データ `questions.js` は前回検証済みリリースから変更していない。

- 問題数: 400
- 模擬試験: 10回 × 40問
- 分野数: 21
- 問題構造検証: エラー0
- 前版とのSHA-256一致: `056beacbd30b3563cc738ef0a73fa980b166d88c3e1ac5610120dd299cda4738`

したがって今回のUX改修による問題文・選択肢・正解・解説への変更はない。

## 7. 実行環境上の制約

今回の実行環境ではChromiumへのページナビゲーションが管理ポリシーでブロックされたため、v4差分のブラウザ実画面E2Eは実行できなかった。

代わりに、実際の `questions.js` → `logic.js` → `app.js` を同一JavaScriptランタイム上でロードし、DOM・localStorage・ユーザー操作を模擬した結合試験で起動・画面描画・イベント処理・保存復元を通している。前版では同一UI構成について390×844px Chromium操作試験を実施済みで、今回の主な変更範囲は起動状態管理、プロフィールUI、キャッシュ更新処理である。

公開後は実機Safari/Chromeで「新規アクセス→入力なしでホーム」「再起動→同じプロフィール」「今日の5問」の3点を最終確認するのが望ましい。

## 総合結果

- 単体: **10/10 PASS**
- 起動・状態管理結合: **18/18 PASS**
- 全体フロー結合: **7/7 PASS**
- PWA/静的構成: **PASS**
- 問題データ回帰: **PASS**

**リリース判定: PASS**
